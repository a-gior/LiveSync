# Ignore everything by default
**/*

# Explicitly include only the necessary folders
!out/
!resources/
!webview-ui/public/

# Always keep required extension files
!package.json
!package-lock.json
!README.md
!CHANGELOG.md
!LICENSE
